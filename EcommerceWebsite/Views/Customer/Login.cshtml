
@{
    ViewData["Title"] = "Register";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<div class="span9">
    <ul class="breadcrumb">
        <li><a  asp-controller="home" asp-action="index">Home</a> <span class="divider">/</span></li>
        <li class="active">Login</li>
    </ul>
    <h3> Login</h3>
    <div class="well">
      
        <form class="form-horizontal" asp-controller="customer" asp-action="login" method="post">
            <h4>LogIn</h4>
            @if (ViewBag.error != null)
            {
                <div class="alert alert-error">
                    <p>
                        @ViewBag.error
                    </p>
                </div>
            }
            <div class="control-group">
                <label class="control-label" for="Username">Username <sup>*</sup></label>
                <div class="controls">
                    <input type="text" id="Username" placeholder="Username" name="username">
                </div>
            </div>
            <div class="control-group">
                <label class="control-label" for="Password">Password <sup>*</sup></label>
                <div class="controls">
                    <input type="password" id="Password" placeholder="Password" name="password" />
                </div>
            </div>


            <div class="control-group">
                <div class="controls">
                    <input class="btn btn-large btn-success" type="submit" value="Login" />
                    <br />
                    <a asp-controller="customer" asp-action="register">Register</a>
                </div>
            </div>
        </form>
    </div>

</div>
